#include<stdio.h>
void swap(int *x,int *y){
     int temp=*x;
     *x=*y;
     *y=temp;
 }
 int main(){
     int a=3,b=6;
     printf("Befoure swaping a=%d and b=%d\n",a,b);
     swap(&a,&b);
     printf("After the swaping a=%d and b=%d",a,b);

    return 0;
}
